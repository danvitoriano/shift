if ((x = y)) {
  /* faça a coisa certa */
} else if (x > y) {
  /* faça outra coisa */
} else {
  /* faça outra coisa */
}

// ternário

var status = (idade >= 18) ? "adulto" : "menor de idade";

// Switch

switch (expressao) {
    case valor1:
        // faça algo
        break;
    case valor2:
        // faça algo
        break;
    default:
        // faça outra coisa
    }